<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abc.app.gofapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BrowseActivity" />
        <activity android:name=".VideoDetail" />
        <activity android:name=".VideoPlay" />
        <activity android:name=".MyVideoActivity" />
        <activity android:name=".PopVideoActivity" />
        <activity android:name=".NewVideoActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".MemberUpdateActivity" />
        <activity android:name=".PaymentList" />
        <activity android:name=".FindVideoActivity">

        </activity>
    </application>

</manifest>